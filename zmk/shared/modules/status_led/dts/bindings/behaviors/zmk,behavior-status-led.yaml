description: Uses LEDs to communicate stateful configurations

compatible: "zmk,behavior-status-led"

include: zero_param.yaml

properties:
  status:
    type: string
    required: true
    enum:
      - "battery"
      - "bluetooth"
